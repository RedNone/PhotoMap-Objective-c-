# Uncomment the next line to define a global platform for your project
# platform :ios, '8.0'

target 'PhotoMap(Objective-c)' do
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
end
